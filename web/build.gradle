plugins {
	id("java-library")
	id("maven-publish")
}

dependencies {
	implementation 'com.fasterxml.jackson.core:jackson-annotations:2.15.2'
	testImplementation 'com.fasterxml.jackson.core:jackson-databind:2.15.2'

	api(project(':core'))
	api(project(':config'))
	api(project(':logback-ext'))
}
